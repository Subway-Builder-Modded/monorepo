/**
 * Accent for the caretaker role, deliberately distinct from the registry
 * purple used for plain collaborators (and from the maps blue / mods red type
 * accents) so caretakers read as their own category wherever both appear.
 */
export const CARETAKER_ACCENT = {
  light: "#0d9488",
  dark: "#2dd4bf",
} as const;
