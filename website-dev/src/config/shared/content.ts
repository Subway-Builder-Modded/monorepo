/**
 * Shared content infrastructure constants.
 * Both the docs and updates config modules derive their edit-URL base and
 * content-root values from here so the monorepo path only lives in one place.
 */

export const CONTENT_GITHUB_EDIT_BASE_URL =
  "https://github.com/Subway-Builder-Modded/monorepo/edit/website";

export const CONTENT_ROOT = "content";
