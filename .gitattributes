*.sh text eol=lf
.githooks/* text eol=lf

# Generated by packages/map-loader via esbuild (always LF). Pin the committed
# bundle to LF so the CI drift check compares LF-to-LF on every platform.
railyard/internal/constants/mod_template.js text eol=lf
